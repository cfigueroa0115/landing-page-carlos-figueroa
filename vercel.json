{
  "buildCommand": "ng build",
  "outputDirectory": "dist/landing-page-cv/browser",
  "framework": "angular",
  "functions": {
    "api/**/*.ts": {
      "maxDuration": 15
    }
  },
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api/$1" }
  ]
}
